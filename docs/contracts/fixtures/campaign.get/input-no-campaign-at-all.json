{
  "operation": "campaign.get",
  "version": 1,
  "case": "neither a pin nor an argument names a campaign, so nothing says which one to read",
  "input": { "args": {} },
  "expect": { "invalid": ["", "/campaign"] }
}
